{% extends 'layout.jinja2' %}

{% block content %}
    <div class="container">
        <h1>{{title}}</h1>
        <p>{{description}}</p>
    </div>
    <button>Sign up</button>
    <button>Log in</button>
{% endblock %}